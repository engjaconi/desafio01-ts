/* import { Account } from "./account";

export class PeopleAccount extends Account {
    doc_id: number;

    constructor(doc_id:number, name:string, accountNumber:number) {
        // Super para definir os atributos da herdados.
        super(name, accountNumber);
        this.doc_id = doc_id;
    }
} */