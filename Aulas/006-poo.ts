/* class User {
    name: string = '';
    age: number = 0;

    constructor(name:string, age:number) {
        this.name = name;
        this.age = age;
    }

    showName = () => {
        return console.log(this.name);
    }
}

const user: User = new User('Tiago', 27);
user.showName();
const otherUser: User = new User('João', 28);
otherUser.showName(); */