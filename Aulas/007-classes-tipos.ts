/* class Account {
    name: string;
    accountNumber: number;

    constructor(name: string, accountNumber: number) {
        this.name = name;
        this.accountNumber = accountNumber;
    }

    deposit = () => {
        console.log('Você depositou');
    }

    withdraw = () => {
        console.log('Você sacou');
    }
}

const newAccount: Account = new Account('Tiago', 1);
console.log(newAccount);
const accout: Account = new Account('Marcos', 26);
accout.deposit();
accout.withdraw(); */